import { HttpStatus } from '../http/http.enum';
export declare class CnpjaException extends Error {
    code: HttpStatus;
    message: string;
    constraints: string[];
    constructor(code: HttpStatus, message: string, constraints: string[]);
}
